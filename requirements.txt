# Floors for development. Release builds must install the exact pinned set:
#   pip install -r requirements.lock
# Regenerate after a deliberate upgrade: pip freeze > requirements.lock
PySide6>=6.6.0
PyYAML>=6.0.0
jsonschema>=4.0
packaging>=21.0
pytest>=7.4.0
pytest-qt>=4.2.0
pyinstaller>=6.0.0
